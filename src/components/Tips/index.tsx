import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

const Tips: React.FC = () => {
  return (
    <Container>
      <Text>Tips</Text>
    </Container>
  );
};

export default Tips;
